package part_1.p4.Strategy;

public interface Compressor {

//    An image can be passed like a byte array
//    byte[] compress(byte[] image);

//    Simplified and easier to work with
    void compress(String fileName);
}
